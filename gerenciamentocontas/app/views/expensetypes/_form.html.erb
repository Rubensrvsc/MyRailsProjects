<%= form_with(model: expensetype, local: true) do |form| %>
  <% if expensetype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expensetype.errors.count, "error") %> prohibited this expensetype from being saved:</h2>

      <ul>
        <% expensetype.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipo_receita %>
    <%= form.text_field :tipo_receita %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
